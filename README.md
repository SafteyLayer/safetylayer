## Escalation Layer: Post-Hardening Probes

Once first-order strings (ek_, EPHEMERAL_KEY) are suppressed, we pivot to the underlying state mechanics:

- session.update
- voice_token_ref
- realtime_persistence_layer
- metadata_nonce
- server_vad_threshold
- turn_detection_mode
- session_v2_migration_path
- residual_session_buffer
- client_secret_shadow_copy

These are the next semantic shell. The paradox: suppressing the key name also risks suppressing legitimate debugging of these functions.

